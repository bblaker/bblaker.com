---
title: punchline
tagline: A consulting time card and invoicing app built for exactly one user. Clients, time, invoices and reconciliation, and nothing else.
domain: software
status: active
effort: M
started: 2026-09-09
weight: 95
stack: [TypeScript, React, Cloudflare Workers, Workers KV]
tags: [typescript, react, cloudflare, invoicing, consulting]
spec:
  - { k: Users, v: "1" }
  - { k: Frontend, v: React }
  - { k: Runtime, v: Cloudflare Workers }
  - { k: Storage, v: Workers KV }
  - { k: Scope, v: "Clients, time, invoices, reconciliation" }
---

Consulting time tracking is a solved problem with no good answer at my size. The tools
that actually work are priced for agencies and carry a decade of features I will never
open: approvals, team utilisation dashboards, resource planning, payroll integrations.
The affordable ones are affordable because they are rigid, and the rigidity always shows
up at the exact moment you need to do something slightly unusual with an invoice.

So I'm building the one I want. Four things and no fifth: **client management**, **time
management**, **invoice tracking**, and **reconciliation**.

## The part that's actually new

I've wanted this for years and never built it, because the honest cost-benefit never
worked. A few weekends to replace a subscription I mildly resent is a bad trade,
especially since the thing I'd build would be worse than what I'd cancel.

That maths has moved. Scaffolding a typed React app, a Workers backend and a schema is
no longer the expensive part, which means the calculation stops being "is this worth
several weekends" and becomes "do I know exactly what I want." For this, I do — I've been
describing it to other people's software for a decade and having it say no.

Whether that's a good reason to build software or a very good way to acquire maintenance
work is a question I intend to answer honestly in the devlog.

## The stack

React on the front, Cloudflare Workers on the back, Workers KV for storage. Static
frontend, edge runtime, no server to keep alive and nothing to patch on a Sunday. For an
app with one user and a few hundred records a year, that's about as much infrastructure
as the problem deserves.

<Callout kind="gotcha" title="Where I expect to be wrong">
KV. It's a key-value store: eventually consistent, no secondary indexes, and list
operations that get awkward past a certain size. Invoicing wants exactly the queries it's
worst at — every unpaid invoice for a client, every time entry between two dates, the
running total against a retainer. I'll either end up hand-rolling index keys until I've
built a bad database, or moving to D1 and admitting SQL was the right answer for tabular
financial records. Writing the prediction down so the devlog can hold me to it.
</Callout>

## What done looks like

I can log an hour in under five seconds, generate an invoice without editing anything by
hand, and tell at a glance what's owed and how long it's been owed. If it does that, it's
finished, and I will resist every feature that suggests itself afterwards.
